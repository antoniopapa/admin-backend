version: "2"
options:
  compose-dev-file-path: ['docker-compose.yaml', 'docker-compose-sync.yml']
syncs:
  admin-sync:
    src: './'
    host_disk_mount_mode: 'cached'
    sync_excludes:
      - '.docker-sync'
      - '.git'
      - '.idea'
      - 'docker'
